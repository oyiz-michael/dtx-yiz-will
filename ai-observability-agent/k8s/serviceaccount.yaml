apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-observability-agent
  namespace: monitoring
  annotations:
    # Replace <ACCOUNT_ID> with your AWS account ID (e.g. 427613144745)
    eks.amazonaws.com/role-arn: arn:aws:iam::<ACCOUNT_ID>:role/ai-observability-agent-role
